# URL of the backend Socket.IO server (required for Netlify/static deployments).
# Leave empty to connect to the same origin (works when served by server.js directly).
VITE_SOCKET_URL=https://your-backend-server.example.com
